# ImageGallery
work in progress

display image from api
when image clicked open more detailed image(bigger) and details (title)

problem
when image clicked, all image came out, not the one with the same data as the clicked one.
